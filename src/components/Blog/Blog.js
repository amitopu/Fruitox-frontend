import React from "react";
import Header from "../Header/Header";

const Blog = () => {
    return (
        <div>
            <Header></Header>
            <p>Welcome to the blog</p>
        </div>
    );
};

export default Blog;
